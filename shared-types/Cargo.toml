[package]
name = "delta-t-zaman-shared"
version = "0.1.0"
edition = "2021"

[lib]
name = "delta_t_zaman_shared"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
